"use strict"; //treat all js code as newer verison but in in general at is already added when you use 
// alert(3 + 3)// we are using nodejs, not browser

//datatype number, bigint,  string, boolean, null => standlone value, undefined => abhi value assing nhi hui hia,  symbol => unique

//object
console.log(typeof null); //object
console.log(typeof undefined); //undefiend